import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'matrix-solver',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
